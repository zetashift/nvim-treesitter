[
  (kw_then)
  (cases)
  (kw_equals)
  (kw_else)
  (do)
] @indent.begin

(literal_list) @indent.begin

(tuple_or_parenthesized
  "(") @indent.begin

(record) @indent.begin

(record
  "}" @indent.branch @indent.end)

(literal_text) @indent.auto
